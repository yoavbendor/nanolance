# Never commit or ship this tree in source archives — it holds FetchContent clones
# (nanoarrow/zstd/CLI11 with .git) plus objects; often 100MB+ while the library sources are ~1–2MB.
/build/
/.cache/
*.rej
CMakeLists.txt.user
compile_commands.json
